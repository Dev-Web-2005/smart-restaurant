import { User } from 'src/common/entities/user';
import { Repository } from 'typeorm';
import LoginAuthRequestDto from 'src/auth/dtos/request/login-auth-request.dto';
import LoginAuthResponseDto from 'src/auth/dtos/response/login-auth-response.dto';
import { GetUserResponseDto } from 'src/users/dtos/response/get-user-response.dto';
import { LogoutAuthRequestDto } from 'src/auth/dtos/request/logout-auth.request.dto';
import { RemoveToken } from 'src/common/entities/remove-token';
import { JwtService } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { ValidateTokenRequestDto } from 'src/auth/dtos/request/validate-token-request.dto';
import { ValidateTokenResponseDto } from 'src/auth/dtos/response/validate-token-response.dto';
import { RefreshTokenResponseDto } from 'src/auth/dtos/response/refresh-token-response.dto';
import { JwtPayload } from '@shared/types';
export declare class AuthService {
    private readonly userRepository;
    private readonly removeTokenRepository;
    private readonly jwtService;
    private readonly configService;
    constructor(userRepository: Repository<User>, removeTokenRepository: Repository<RemoveToken>, jwtService: JwtService, configService: ConfigService);
    private readonly ACCESS_TOKEN_EXPIRY;
    private readonly REFRESH_TOKEN_EXPIRY;
    login(data: LoginAuthRequestDto): Promise<LoginAuthResponseDto>;
    private generateAccessToken;
    generateRefreshToken(payload: Omit<JwtPayload, 'type'>): string;
    validateToken(data: ValidateTokenRequestDto): Promise<ValidateTokenResponseDto>;
    refreshAccessToken(refreshToken: string): Promise<ValidateTokenResponseDto>;
    getUserFromRefreshToken(refreshToken: string): Promise<RefreshTokenResponseDto>;
    private verifyAccessToken;
    private verifyRefreshToken;
    private isTokenBlacklisted;
    me(userId: string): Promise<Omit<GetUserResponseDto, 'roles'> | null>;
    logout(data: LogoutAuthRequestDto): Promise<void>;
}
